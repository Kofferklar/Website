---
phase: 02-produktseite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(site)/produkt/components/ProductGallery.tsx
  - app/(site)/produkt/components/ProductVideo.tsx
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - DESIGN-03

must_haves:
  truths:
    - "ProductGallery.tsx existiert unter `app/(site)/produkt/components/` und ist ein Client Component (`'use client'`)"
    - "Galerie zeigt Hauptbild (groß) + mind. 3 Thumbnail-Bilder darunter; Klick auf Thumbnail tauscht Hauptbild"
    - "Bilder nutzen `next/image` mit `placeholder=\"blur\"` und `blurDataURL`"
    - "ProductVideo.tsx existiert und rendert entweder `<video>` (direkte URL) oder YouTube-Embed (iframe) je nach URL-Pattern"
    - "Beide Komponenten akzeptieren Props aus dem Sanity `Product`-Interface (SanityImage[], videoUrl?)"
    - "`npm run build` exits 0 — keine TypeScript-Fehler"
  artifacts:
    - path: "app/(site)/produkt/components/ProductGallery.tsx"
      provides: "Interaktive Bildergalerie mit Thumbnail-Navigation"
      contains: "'use client'"
    - path: "app/(site)/produkt/components/ProductVideo.tsx"
      provides: "Video-Embed-Komponente für YouTube oder direkte Video-URL"
      contains: "videoUrl"
  key_links:
    - from: "app/(site)/produkt/components/ProductGallery.tsx"
      to: "lib/sanity/types.ts"
      via: "SanityImage interface import"
      pattern: "SanityImage"
    - from: "app/(site)/produkt/components/ProductGallery.tsx"
      to: "lib/sanity/image.ts"
      via: "urlFor() builder"
      pattern: "urlFor"
---

<objective>
`ProductGallery` und `ProductVideo` als eigenständige Client Components bauen.

ProductGallery: Zeigt ein großes Hauptbild und darunter 3–8 Thumbnails. Klick auf Thumbnail setzt activeIndex, Hauptbild wechselt smooth mit opacity-Transition. Nutzt `next/image` + `placeholder="blur"`. Touch/Swipe auf Mobile (Framer Motion `drag="x"`) optional aber empfohlen.

ProductVideo: Erkennt URL-Typ automatisch — enthält "youtube.com" oder "youtu.be" → responsive iframe-Wrapper; sonst → `<video controls>` Element. Beide Varianten responsive.

Purpose: Die zwei interaktivsten Komponenten der Produktseite bauen — Galerie und Video bestimmen den ersten Eindruck des Produkts.

Output: Zwei fertige, testbare Client Components in `app/(site)/produkt/components/`.
</objective>

<execution_context>
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.planning/ROADMAP.md
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.planning/REQUIREMENTS.md
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.planning/phases/01-foundation-cms/01-CONTEXT.md

<interfaces>
<!-- Bestehende Typen aus lib/sanity/types.ts -->

```typescript
// SanityImage (vollständig)
export interface SanityImage {
  _type: 'image'
  asset: { _ref: string; _type: 'reference' }
  hotspot?: { x: number; y: number; height: number; width: number }
  alt?: string
}

// Product.images ist SanityImage[]
// Product.videoUrl ist string | undefined
```

<!-- Bestehender urlFor Builder aus lib/sanity/image.ts -->
```typescript
import { urlFor } from '@/lib/sanity/image'
// urlFor(image).width(800).height(600).url()
// urlFor(image).width(64).height(64).url()
```

<!-- Tailwind-Token (aus tailwind.config.ts) -->
// primary: #1E3A5F (Navy)
// accent: #C9A84C (Gold)
// background: #FAFAF8 (off-white)
// foreground: #1A1A1A (near-black)
// muted: #F2F2F0 (sekundäre Fläche)
// border: rgba(0,0,0,0.08)

<!-- Blur placeholder für next/image -->
// Für Sanity-Bilder ohne echte blurDataURL: statische Base64-Placeholder-URL verwenden
// Minimal 1px gray: "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg=="
// Oder: next/image blur mit `blurDataURL` Property setzen, wenn Sanity `lqip` nicht verfügbar ist
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProductGallery Client Component</name>
  <read_first>
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/lib/sanity/types.ts (SanityImage interface — Props-Typ)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/lib/sanity/image.ts (urlFor Builder — Bildgenerierung)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/tailwind.config.ts (Farb-Tokens — kein Hardcoding von Hex-Werten)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/.agents/skills/high-end-visual-design/SKILL.md (Design-Regeln)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/.agents/skills/design-taste-frontend/SKILL.md (Component-Architektur-Regeln)
  </read_first>
  <files>
    app/(site)/produkt/components/ProductGallery.tsx
  </files>
  <action>
Erstelle `app/(site)/produkt/components/ProductGallery.tsx` als Client Component.

**Verzeichnis anlegen:**
```bash
mkdir -p "app/(site)/produkt/components"
```

**Komponenten-Signatur:**
```typescript
'use client'

import Image from 'next/image'
import { useState } from 'react'
import { urlFor } from '@/lib/sanity/image'
import type { SanityImage } from '@/lib/sanity/types'

interface ProductGalleryProps {
  images: SanityImage[]
  productName: string
}

export default function ProductGallery({ images, productName }: ProductGalleryProps)
```

**Layout-Struktur:**
- Äußerer Container: `w-full` — nimmt volle Breite des übergeordneten Grid-Containers
- Hauptbild-Container: `relative aspect-square md:aspect-[4/3] w-full overflow-hidden rounded-xl bg-muted`
- Hauptbild: `next/image` mit `fill`, `object-cover`, `priority` (above-the-fold), `placeholder="blur"`, `blurDataURL="data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg=="`
- Bildwechsel-Transition: `transition-opacity duration-300` auf dem Bild-Element
- Thumbnail-Grid darunter: `mt-3 grid grid-cols-4 gap-2` (max. 4 Thumbnails sichtbar, rest ausgeblendet)
- Thumbnail-Button: `relative aspect-square overflow-hidden rounded-lg cursor-pointer ring-2 ring-transparent hover:ring-accent transition-all duration-200` — aktiver Thumbnail: `ring-primary`
- Thumbnail-Bild: `next/image` mit `fill`, `object-cover`, `sizes="80px"`

**State:**
```typescript
const [activeIndex, setActiveIndex] = useState(0)
```

**Klick-Handler:**
```typescript
const handleThumbnailClick = (index: number) => setActiveIndex(index)
```

**Fallback:** Falls `images` leer ist oder `undefined`, Placeholder-Box rendern:
```tsx
if (!images || images.length === 0) {
  return (
    <div className="w-full aspect-square md:aspect-[4/3] rounded-xl bg-muted flex items-center justify-center">
      <span className="text-muted-foreground text-sm">Kein Bild verfügbar</span>
    </div>
  )
}
```

**Bildgenerierung mit urlFor:**
- Hauptbild: `urlFor(images[activeIndex]).width(800).height(600).url()`
- Thumbnail: `urlFor(image).width(160).height(160).url()`
- IMMER Null-Check: `images[activeIndex]?.asset ? urlFor(...) : null`

**Accessibility:**
- Hauptbild: `alt={images[activeIndex]?.alt ?? productName}`
- Thumbnail-Button: `aria-label={`Bild ${index + 1} von ${images.length} anzeigen`}`, `aria-pressed={index === activeIndex}`

**Kein Framer Motion in diesem Task** — einfache CSS-Transition reicht für Phase 2.
  </action>
  <verify>
    <automated>cd /Users/nicopapandrockndrock/wavyy/KofferKlar && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `app/(site)/produkt/components/ProductGallery.tsx` existiert (grep: `test -f "app/(site)/produkt/components/ProductGallery.tsx"`)
    - Datei enthält `'use client'` als erste Zeile
    - Datei enthält `useState` Import
    - Datei enthält `urlFor` Import aus `@/lib/sanity/image`
    - Datei enthält `SanityImage` Import aus `@/lib/sanity/types`
    - Datei enthält `placeholder="blur"` (PROD-01, DESIGN-03 erfüllt)
    - Datei enthält `aria-label` an Thumbnail-Buttons (Accessibility)
    - `npx tsc --noEmit` exits 0 (keine TypeScript-Fehler)
  </acceptance_criteria>
  <done>ProductGallery Client Component mit Thumbnail-Navigation, aktiver Bild-State, Blur-Placeholder und Accessibility-Attributen.</done>
</task>

<task type="auto">
  <name>Task 2: ProductVideo Client Component</name>
  <read_first>
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/app/(site)/produkt/components/ProductGallery.tsx (Stil-Konsistenz — gleicher rounded-xl, bg-muted)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/sanity/schemaTypes/productType.ts (videoUrl field — type: 'url')
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/lib/sanity/types.ts (Product.videoUrl: string | undefined)
  </read_first>
  <files>
    app/(site)/produkt/components/ProductVideo.tsx
  </files>
  <action>
Erstelle `app/(site)/produkt/components/ProductVideo.tsx` als Client Component.

**Komponenten-Signatur:**
```typescript
'use client'

interface ProductVideoProps {
  videoUrl: string
  title?: string
}

export default function ProductVideo({ videoUrl, title = 'Produktvideo' }: ProductVideoProps)
```

**URL-Erkennung:**
```typescript
function isYouTubeUrl(url: string): boolean {
  return url.includes('youtube.com') || url.includes('youtu.be')
}

function getYouTubeEmbedUrl(url: string): string {
  // Patterns: youtube.com/watch?v=ID, youtu.be/ID, youtube.com/embed/ID
  const match = url.match(/(?:youtube\.com\/(?:watch\?v=|embed\/)|youtu\.be\/)([a-zA-Z0-9_-]{11})/)
  return match ? `https://www.youtube-nocookie.com/embed/${match[1]}` : url
}
```

**YouTube-Embed:**
```tsx
<div className="relative w-full aspect-video rounded-xl overflow-hidden bg-muted">
  <iframe
    src={getYouTubeEmbedUrl(videoUrl)}
    title={title}
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
    allowFullScreen
    className="absolute inset-0 w-full h-full"
    loading="lazy"
  />
</div>
```

**Direkte Video-URL:**
```tsx
<div className="relative w-full aspect-video rounded-xl overflow-hidden bg-muted">
  <video
    controls
    preload="metadata"
    className="w-full h-full object-cover"
    aria-label={title}
  >
    <source src={videoUrl} />
    Ihr Browser unterstützt dieses Video-Format nicht.
  </video>
</div>
```

**Privacy:** Nutze `youtube-nocookie.com` statt `youtube.com` für YouTube-Embeds (DSGVO-freundlicher).

**Render-Entscheidung:**
```typescript
if (isYouTubeUrl(videoUrl)) {
  return <YouTubeEmbed ... />
}
return <DirectVideo ... />
```
  </action>
  <verify>
    <automated>cd /Users/nicopapandrockndrock/wavyy/KofferKlar && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `app/(site)/produkt/components/ProductVideo.tsx` existiert
    - Datei enthält `'use client'`
    - Datei enthält `youtube-nocookie.com` (Privacy-konformes YouTube-Embed)
    - Datei enthält `aspect-video` (responsives 16:9 Verhältnis)
    - Datei enthält `allowFullScreen` (Vollbild für iframe)
    - Datei enthält `controls` (Video-Controls für direktes Video)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ProductVideo Client Component mit automatischer URL-Erkennung (YouTube iframe vs. direktes Video), responsivem 16:9-Container und DSGVO-freundlichem youtube-nocookie.com-Embed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity CDN → next/image | URL-Generierung via urlFor() — keine User-Input-URLs |
| External videoUrl → iframe | Sanity-Schema validiert URL-Typ; trotzdem pattern-matching für YouTube |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | YouTube-Embed | mitigate | Nutze `youtube-nocookie.com` statt `youtube.com` — verhindert Tracking ohne Nutzereinwilligung |
| T-02-02 | Tampering | videoUrl über Sanity | accept | Sanity-Schema validiert URL-Format; Editor-Zugang ist Basic-Auth-geschützt |
| T-02-03 | XSS | iframe src | accept | URL kommt aus Sanity (vertrauenswürdige Quelle), nicht aus User-Input; `allow` Attribut beschränkt Capabilities |
</threat_model>

<verification>
Nach Task 1 + 2:
```bash
cd /Users/nicopapandrockndrock/wavyy/KofferKlar

# Komponentendateien vorhanden
test -f "app/(site)/produkt/components/ProductGallery.tsx" && echo "GALLERY: OK"
test -f "app/(site)/produkt/components/ProductVideo.tsx" && echo "VIDEO: OK"

# Client Component marker
grep -l "'use client'" "app/(site)/produkt/components/ProductGallery.tsx" "app/(site)/produkt/components/ProductVideo.tsx"

# TypeScript
npx tsc --noEmit 2>&1 | tail -5

# Build
npm run build 2>&1 | tail -10
```
</verification>

<success_criteria>
- `ProductGallery.tsx` und `ProductVideo.tsx` in `app/(site)/produkt/components/` vorhanden
- Beide Dateien beginnen mit `'use client'`
- Galerie: `useState`, `urlFor`, `placeholder="blur"`, `aria-label` auf Thumbnails
- Video: `youtube-nocookie.com`, `aspect-video`, `allowFullScreen`
- `npm run build` exits 0
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/02-produktseite/02-01-SUMMARY.md` erstellen
</output>
