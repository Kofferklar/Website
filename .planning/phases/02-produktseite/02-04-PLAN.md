---
phase: 02-produktseite
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - app/(site)/produkt/components/ProductReviews.tsx
  - app/(site)/produkt/components/ProductFaq.tsx
autonomous: true
requirements:
  - PROD-10
  - PROD-11

must_haves:
  truths:
    - "ProductReviews.tsx ist ein Server Component das Durchschnittssterne, Gesamtanzahl und einzelne Review-Cards rendert"
    - "ProductFaq.tsx ist ein Client Component mit Accordion-Verhalten (öffnen/schließen per Klick, ohne Seitenreload)"
    - "FAQ-Accordion zeigt mind. die ersten 3 Fragen — alle FAQ-Items aus Sanity werden gerendert"
    - "Sterne-Darstellung nutzt echte Star-Icons (gefüllt/leer) nicht nur Text"
    - "`npm run build` exits 0"
  artifacts:
    - path: "app/(site)/produkt/components/ProductReviews.tsx"
      provides: "Review-Sektion: Durchschnitt, Anzahl, Review-Cards mit Sterne-Rating"
      contains: "reviewerName"
    - path: "app/(site)/produkt/components/ProductFaq.tsx"
      provides: "Accordion-FAQ aus Sanity faqItem-Schema"
      contains: "'use client'"
  key_links:
    - from: "app/(site)/produkt/components/ProductReviews.tsx"
      to: "lib/sanity/types.ts"
      via: "Review interface"
      pattern: "Review"
    - from: "app/(site)/produkt/components/ProductFaq.tsx"
      to: "lib/sanity/types.ts"
      via: "FaqItem interface"
      pattern: "FaqItem"
---

<objective>
`ProductReviews` und `ProductFaq` bauen — die beiden sozialen Überzeugungselemente:

1. **ProductReviews** (Server Component): Zeigt Durchschnittssterne (berechnet aus `rating`-Feldern), Gesamtanzahl und scrollbare Review-Cards (Name, Datum, Sterne, Text). Verified-Badge wenn `review.verified === true`.

2. **ProductFaq** (Client Component): Accordion mit allen FAQ-Items aus Sanity. Jede Frage als Zeile mit Pfeil-Icon, Klick öffnet/schließt Antwort mit `max-height`-Transition. ARIA-Attribute für Accessibility. Items nach `category` gruppiert (optional).

Purpose: Vertrauen aufbauen. Reviews und FAQ reduzieren Kaufzögern — die häufigsten Einwände werden direkt adressiert.

Output: Zwei fertige Komponenten die direkt in page.tsx integriert werden können.
</objective>

<execution_context>
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.planning/ROADMAP.md
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.planning/REQUIREMENTS.md

<interfaces>
<!-- Review Interface aus lib/sanity/types.ts -->
```typescript
export interface Review {
  _id: string
  reviewerName: string
  rating: number        // 1–5
  body: string
  publishedAt?: string
  verified?: boolean
}
```

<!-- FaqItem Interface aus lib/sanity/types.ts -->
```typescript
export interface FaqItem {
  _id: string
  question: string
  answer: string
  category: string
}
```

<!-- Queries aus lib/sanity/queries.ts -->
// getAllReviews() → Review[]
// getFaqItems() → FaqItem[]

<!-- Datum-Formatierung für publishedAt -->
// ISO-String → "April 2026" oder "12. April 2026"
// new Date(publishedAt).toLocaleDateString('de-DE', { month: 'long', year: 'numeric' })

<!-- Sterne-Rendering -->
// Für Sterne: Star-Icon aus lucide-react
// import { Star } from 'lucide-react'
// Gefüllt: className="fill-accent text-accent"
// Leer: className="fill-none text-muted"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProductReviews Server Component</name>
  <read_first>
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/lib/sanity/types.ts (Review interface)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/lib/sanity/queries.ts (getAllReviews — nicht in diesem Kontext importieren, Daten kommen als Props)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/.agents/skills/high-end-visual-design/SKILL.md (Card-Design, Sterne-Styling)
  </read_first>
  <files>
    app/(site)/produkt/components/ProductReviews.tsx
  </files>
  <action>
Erstelle `app/(site)/produkt/components/ProductReviews.tsx` als Server Component.

**Props-Interface:**
```typescript
import { Star, CheckCircle2 } from 'lucide-react'
import type { Review } from '@/lib/sanity/types'

interface ProductReviewsProps {
  reviews: Review[]
}
```

**Helper-Funktionen:**
```typescript
function getAverageRating(reviews: Review[]): number {
  if (!reviews.length) return 0
  const sum = reviews.reduce((acc, r) => acc + r.rating, 0)
  return Math.round((sum / reviews.length) * 10) / 10 // Eine Dezimalstelle
}

function StarRating({ rating, size = 'sm' }: { rating: number; size?: 'sm' | 'lg' }) {
  const sizeClass = size === 'lg' ? 'w-6 h-6' : 'w-4 h-4'
  return (
    <div className="flex items-center gap-0.5" aria-label={`${rating} von 5 Sternen`}>
      {[1, 2, 3, 4, 5].map((star) => (
        <Star
          key={star}
          className={`${sizeClass} ${star <= rating ? 'fill-accent text-accent' : 'fill-muted text-muted'}`}
          aria-hidden="true"
        />
      ))}
    </div>
  )
}

function formatDate(dateStr?: string): string {
  if (!dateStr) return ''
  return new Date(dateStr).toLocaleDateString('de-DE', { month: 'long', year: 'numeric' })
}
```

**Render-Struktur:**
```tsx
export default function ProductReviews({ reviews }: ProductReviewsProps) {
  if (!reviews || reviews.length === 0) return null
  
  const avg = getAverageRating(reviews)

  return (
    <section aria-label="Kundenbewertungen">
      <h2 className="font-serif text-2xl md:text-3xl font-bold text-foreground mb-8">
        Was Kunden sagen
      </h2>

      {/* Durchschnitt-Summary */}
      <div className="flex items-center gap-6 mb-10 p-6 bg-muted rounded-2xl">
        <div className="text-center">
          <div className="font-serif text-5xl font-bold text-foreground">{avg}</div>
          <StarRating rating={Math.round(avg)} size="lg" />
          <p className="text-sm text-muted-foreground mt-1">{reviews.length} Bewertungen</p>
        </div>
        <div className="h-16 w-px bg-border" aria-hidden="true" />
        <p className="text-sm text-muted-foreground">
          Verifizierte Käufer · Alle Bewertungen stammen von echten Kunden
        </p>
      </div>

      {/* Review-Cards Grid */}
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {reviews.map((review) => (
          <article
            key={review._id}
            className="bg-background border border-border rounded-2xl p-6 flex flex-col gap-3"
          >
            {/* Header: Name + Verified */}
            <div className="flex items-start justify-between gap-2">
              <div>
                <p className="font-semibold text-foreground text-sm">{review.reviewerName}</p>
                {review.publishedAt && (
                  <p className="text-xs text-muted-foreground">{formatDate(review.publishedAt)}</p>
                )}
              </div>
              {review.verified && (
                <div className="flex items-center gap-1 text-xs text-primary flex-shrink-0">
                  <CheckCircle2 className="w-3.5 h-3.5" aria-hidden="true" />
                  <span>Verifiziert</span>
                </div>
              )}
            </div>

            {/* Sterne */}
            <StarRating rating={review.rating} />

            {/* Review-Text */}
            <p className="text-sm text-foreground leading-relaxed flex-1">
              &ldquo;{review.body}&rdquo;
            </p>
          </article>
        ))}
      </div>
    </section>
  )
}
```
  </action>
  <verify>
    <automated>cd /Users/nicopapandrockndrock/wavyy/KofferKlar && grep -c "StarRating" "app/(site)/produkt/components/ProductReviews.tsx" && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `app/(site)/produkt/components/ProductReviews.tsx` existiert
    - Datei enthält NICHT `'use client'` (Server Component)
    - Datei enthält `fill-accent text-accent` (gefüllte Sterne in Gold)
    - Datei enthält `aria-label` mit Sternbeschreibung
    - Datei enthält `review.verified` Behandlung (Verified-Badge)
    - Datei enthält `getAverageRating` oder ähnliche Durchschnitt-Berechnung
    - Datei enthält `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` (responsives Grid)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ProductReviews Server Component: Durchschnitts-Summary, Sterne-Grid, Review-Cards mit Verified-Badge und formatiertem Datum.</done>
</task>

<task type="auto">
  <name>Task 2: ProductFaq Client Component mit Accordion</name>
  <read_first>
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/lib/sanity/types.ts (FaqItem interface)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/app/(site)/produkt/components/ProductReviews.tsx (Stil-Konsistenz — gleiche Border-Tokens)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/.agents/skills/design-taste-frontend/SKILL.md (Accordion-Pattern)
  </read_first>
  <files>
    app/(site)/produkt/components/ProductFaq.tsx
  </files>
  <action>
Erstelle `app/(site)/produkt/components/ProductFaq.tsx` als Client Component.

**Props:**
```typescript
'use client'

import { useState } from 'react'
import { ChevronDown } from 'lucide-react'
import type { FaqItem } from '@/lib/sanity/types'

interface ProductFaqProps {
  items: FaqItem[]
  title?: string
}
```

**State:** `openId: string | null` — welche FAQ-ID aktuell geöffnet ist. Nur eine FAQ gleichzeitig offen (Accordion-Pattern).

**Accordion-Item Komponente:**
```typescript
function FaqAccordionItem({ item, isOpen, onToggle }: {
  item: FaqItem
  isOpen: boolean
  onToggle: () => void
}) {
  const answerId = `faq-answer-${item._id}`
  const buttonId = `faq-button-${item._id}`

  return (
    <div className="border-b border-border last:border-b-0">
      <button
        id={buttonId}
        className="w-full flex items-center justify-between gap-4 py-5 text-left"
        onClick={onToggle}
        aria-expanded={isOpen}
        aria-controls={answerId}
      >
        <span className="font-medium text-foreground text-sm md:text-base pr-4">
          {item.question}
        </span>
        <ChevronDown
          className={`w-5 h-5 text-muted-foreground flex-shrink-0 transition-transform duration-200 ${isOpen ? 'rotate-180' : 'rotate-0'}`}
          aria-hidden="true"
        />
      </button>

      {/* Antwort — max-height Transition */}
      <div
        id={answerId}
        role="region"
        aria-labelledby={buttonId}
        className="overflow-hidden transition-all duration-300 ease-in-out"
        style={{ maxHeight: isOpen ? '500px' : '0px' }}
      >
        <div className="pb-5 text-sm text-muted-foreground leading-relaxed">
          {item.answer}
        </div>
      </div>
    </div>
  )
}
```

**Haupt-Render:**
```typescript
export default function ProductFaq({ items, title = 'Häufige Fragen' }: ProductFaqProps) {
  const [openId, setOpenId] = useState<string | null>(items[0]?._id ?? null)

  const handleToggle = (id: string) => {
    setOpenId(prev => prev === id ? null : id)
  }

  if (!items || items.length === 0) return null

  return (
    <section aria-label="FAQ">
      <h2 className="font-serif text-2xl md:text-3xl font-bold text-foreground mb-8">
        {title}
      </h2>
      <div className="divide-y-0 border border-border rounded-2xl overflow-hidden">
        <div className="px-6 divide-y divide-border">
          {items.map((item) => (
            <FaqAccordionItem
              key={item._id}
              item={item}
              isOpen={openId === item._id}
              onToggle={() => handleToggle(item._id)}
            />
          ))}
        </div>
      </div>
    </section>
  )
}
```

**WICHTIG:**
- `aria-expanded` auf dem Button (PROD-11: öffnen/schließen ohne Seitenreload)
- `aria-controls` verknüpft Button mit Antwort-Div
- `role="region"` + `aria-labelledby` auf dem Antwort-Div
- Erste FAQ standardmäßig geöffnet (`useState(items[0]?._id ?? null)`)
- `max-height: 500px` als offener Zustand (ausreichend für lange Antworten)
- CSS-Transition statt Framer Motion — einfacher und performanter für einfache Akkordeons
  </action>
  <verify>
    <automated>cd /Users/nicopapandrockndrock/wavyy/KofferKlar && grep -c "aria-expanded" "app/(site)/produkt/components/ProductFaq.tsx" && grep -c "aria-controls" "app/(site)/produkt/components/ProductFaq.tsx" && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `app/(site)/produkt/components/ProductFaq.tsx` existiert
    - Datei enthält `'use client'`
    - Datei enthält `useState` für Accordion-State
    - Datei enthält `aria-expanded` (PROD-11 — öffnen/schließen ohne Reload)
    - Datei enthält `aria-controls` (Accessibility-Verknüpfung)
    - Datei enthält `ChevronDown` mit Rotation-Klasse
    - Datei enthält `max-height` Transition (CSS Accordion-Animation)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ProductFaq Client Component: Accessibility-konformes Accordion mit ARIA-Attributen, max-height Transition, erstes Item standardmäßig geöffnet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity CMS → Review.body | Review-Text kommt aus Sanity, wird als Plaintext gerendert (kein innerHTML) |
| Sanity CMS → FaqItem.answer | Gleich — Plaintext, kein XSS-Risiko |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | XSS | Review.body Rendering | accept | Text wird über JSX `{review.body}` gerendert — React escapet automatisch; kein dangerouslySetInnerHTML |
| T-02-09 | XSS | FaqItem.answer Rendering | accept | Gleiche Begründung — JSX-Rendering ist XSS-safe |
</threat_model>

<verification>
Nach Task 1 + 2:
```bash
cd /Users/nicopapandrockndrock/wavyy/KofferKlar

# Dateien vorhanden
test -f "app/(site)/produkt/components/ProductReviews.tsx" && echo "REVIEWS: OK"
test -f "app/(site)/produkt/components/ProductFaq.tsx" && echo "FAQ: OK"

# Server/Client korrekt
grep -L "'use client'" "app/(site)/produkt/components/ProductReviews.tsx"  # Reviews ist Server Component
grep "'use client'" "app/(site)/produkt/components/ProductFaq.tsx"          # FAQ ist Client Component

# ARIA-Attribute
grep "aria-expanded" "app/(site)/produkt/components/ProductFaq.tsx"
grep "aria-controls" "app/(site)/produkt/components/ProductFaq.tsx"

# TypeScript + Build
npx tsc --noEmit && npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
- `ProductReviews.tsx` und `ProductFaq.tsx` in `app/(site)/produkt/components/`
- ProductReviews: Server Component, Durchschnittsberechnung, Sterne mit fill-accent, Verified-Badge
- ProductFaq: Client Component, aria-expanded + aria-controls, max-height Transition
- `npm run build` exits 0
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/02-produktseite/02-04-SUMMARY.md` erstellen
</output>
