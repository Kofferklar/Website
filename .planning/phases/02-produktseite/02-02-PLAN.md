---
phase: 02-produktseite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(site)/produkt/components/BuyBlock.tsx
  - app/(site)/produkt/components/SetOverview.tsx
  - app/(site)/produkt/components/SizeChart.tsx
autonomous: true
requirements:
  - PROD-03
  - PROD-04
  - PROD-05
  - PROD-08
  - PROD-09

must_haves:
  truths:
    - "BuyBlock.tsx ist ein Server Component (kein 'use client') und enthält Preis, CTA-Button, Versandinfo und Zahlungslogos"
    - "BuyBlock CTA-Button hat `target='_blank' rel='noopener noreferrer'` und nimmt `buyLink` als Prop"
    - "BuyBlock ist auf Desktop sticky (`sticky top-24`) — bleibt beim Scrollen sichtbar"
    - "SetOverview.tsx zeigt ein Grid mit allen Set-Teilen (Name + Maße aus `setParts[]`)"
    - "SizeChart.tsx rendert eine `<table>` mit allen Packwürfel-Dimensionen"
    - "Alle 4 Zahlungslogos aus `/public/icons/` werden in BuyBlock als `next/image` eingebunden"
    - "`npm run build` exits 0"
  artifacts:
    - path: "app/(site)/produkt/components/BuyBlock.tsx"
      provides: "Kauf-Conversion-Block mit Preis, CTA, Versandinfo, Zahlungslogos, sticky Desktop"
      contains: "sticky"
    - path: "app/(site)/produkt/components/SetOverview.tsx"
      provides: "8-Teile Set-Übersicht aus Sanity setParts"
      contains: "setParts"
    - path: "app/(site)/produkt/components/SizeChart.tsx"
      provides: "Maßtabelle mit Packwürfel-Dimensionen"
      contains: "<table"
  key_links:
    - from: "app/(site)/produkt/components/BuyBlock.tsx"
      to: "public/icons/paypal.svg"
      via: "next/image src prop"
      pattern: "/icons/paypal"
    - from: "app/(site)/produkt/components/SetOverview.tsx"
      to: "lib/sanity/types.ts"
      via: "SetPart interface"
      pattern: "SetPart"
---

<objective>
Drei Server Components bauen, die den kommerziell kritischsten Teil der Produktseite abbilden:

1. **BuyBlock**: Preis (prominent) + "Jetzt kaufen"-Button (externer Link, target=_blank) + Versandtext + 4 Zahlungslogos. Auf Desktop `sticky top-24` — bleibt beim Scrollen im Viewport. Auf Mobile normal im Flow.

2. **SetOverview**: Grid-Darstellung aller 8 Packwürfel-Teile mit Name und Maßen aus Sanity `setParts[]`. Falls Icon vorhanden: zeigen; sonst: neutrales Box-Icon aus lucide-react.

3. **SizeChart**: Tabellarische Darstellung aller Packwürfel-Dimensionen. Harte Werte eingebaut (nicht aus Sanity — Maße ändern sich nicht). Responsive via horizontalen Scroll auf Mobile.

Purpose: Den Conversion-Pfad vollständig implementieren. BuyBlock ist das wichtigste Element der Produktseite — der Nutzer soll immer sehen können, was das Produkt kostet und wie er es kaufen kann.

Output: Drei fertige Server Components die direkt in page.tsx importiert werden können.
</objective>

<execution_context>
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.planning/ROADMAP.md
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.planning/REQUIREMENTS.md
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.planning/phases/01-foundation-cms/01-CONTEXT.md

<interfaces>
<!-- Relevante Typen aus lib/sanity/types.ts -->
```typescript
export interface SetPart {
  partName: string
  dimensions: string
  icon?: SanityImage
}

export interface Product {
  price?: number
  buyLink?: string
  setParts?: SetPart[]
  material?: string
}
```

<!-- Vorhandene Zahlungs-SVGs (aus 01-04-SUMMARY.md) -->
// public/icons/paypal.svg   ✓
// public/icons/klarna.svg   ✓
// public/icons/visa.svg     ✓
// public/icons/mastercard.svg ✓

<!-- Design-Tokens -->
// primary: #1E3A5F (Navy) — für CTA-Button Hintergrund
// primary-foreground: #FFFFFF — Button-Text
// accent: #C9A84C (Gold) — für Preis-Highlighting (optional)
// background: #FAFAF8
// foreground: #1A1A1A
// muted: #F2F2F0 — für Set-Grid-Karten-Hintergrund
// border: rgba(0,0,0,0.08)

<!-- Versandtext (hartcodiert, kommt nicht aus Sanity) -->
// "Kostenloser Versand ab 49€ · 2–4 Werktage"
// "30 Tage Rückgabe"

<!-- Packwürfel-Maße (für SizeChart — hartcodiert) -->
// XL: 40 × 30 × 12 cm
// L: 35 × 25 × 10 cm
// M: 30 × 20 × 8 cm
// S: 25 × 15 × 7 cm
// Schuhbeutel: 30 × 20 cm (flach)
// Wäschebeutel: 35 × 25 cm (flach)
// Kosmetik-Beutel: 20 × 15 cm (flach)
// Kreditkarten-Beutel: 12 × 9 cm (flach)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BuyBlock Server Component</name>
  <read_first>
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/lib/sanity/types.ts (Product interface — price, buyLink, material)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/app/(site)/components/Header.tsx (CTA-Button-Stil für Konsistenz)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/.agents/skills/high-end-visual-design/SKILL.md
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/.agents/skills/design-taste-frontend/SKILL.md
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/public/icons/ (vorhandene SVG-Dateien bestätigen)
  </read_first>
  <files>
    app/(site)/produkt/components/BuyBlock.tsx
  </files>
  <action>
Erstelle `app/(site)/produkt/components/BuyBlock.tsx` als Server Component (KEIN `'use client'`).

**Props-Interface:**
```typescript
import Image from 'next/image'
import { ShieldCheck, Truck, RotateCcw } from 'lucide-react'

interface BuyBlockProps {
  price?: number
  buyLink?: string
  material?: string
}

export default function BuyBlock({ price, buyLink, material }: BuyBlockProps)
```

**Struktur des BuyBlock:**
```tsx
<div className="sticky top-24 space-y-6">
  {/* Preis */}
  <div>
    <div className="flex items-baseline gap-2">
      <span className="font-serif text-4xl font-bold text-foreground">
        {price ? `${price.toFixed(2).replace('.', ',')} €` : '–'}
      </span>
      <span className="text-sm text-muted-foreground">inkl. MwSt.</span>
    </div>
  </div>

  {/* CTA-Button */}
  <a
    href={buyLink ?? '#'}
    target="_blank"
    rel="noopener noreferrer"
    className="block w-full text-center bg-primary text-primary-foreground px-8 py-4 rounded-full text-base font-semibold hover:bg-primary-600 active:scale-[0.98] transition-all duration-300 focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2"
    style={{ transitionTimingFunction: 'cubic-bezier(0.16, 1, 0.3, 1)' }}
    aria-label="Jetzt kaufen — öffnet externen Shop"
  >
    Jetzt kaufen
  </a>

  {/* Trust-Badges (Versand, Rückgabe, Sicherheit) */}
  <div className="space-y-3 border-t border-border pt-4">
    <div className="flex items-center gap-3 text-sm text-muted-foreground">
      <Truck className="w-4 h-4 flex-shrink-0 text-foreground" aria-hidden="true" />
      <span>Kostenloser Versand ab 49&nbsp;€ · 2–4&nbsp;Werktage</span>
    </div>
    <div className="flex items-center gap-3 text-sm text-muted-foreground">
      <RotateCcw className="w-4 h-4 flex-shrink-0 text-foreground" aria-hidden="true" />
      <span>30&nbsp;Tage Rückgabe — kein Aufwand</span>
    </div>
    <div className="flex items-center gap-3 text-sm text-muted-foreground">
      <ShieldCheck className="w-4 h-4 flex-shrink-0 text-foreground" aria-hidden="true" />
      <span>SSL-gesicherte Zahlung</span>
    </div>
  </div>

  {/* Material (optional — nur wenn vorhanden) */}
  {material && (
    <div className="text-sm text-muted-foreground border-t border-border pt-4">
      <span className="font-medium text-foreground">Material: </span>
      {material}
    </div>
  )}

  {/* Zahlungslogos */}
  <div className="border-t border-border pt-4">
    <p className="text-xs text-muted-foreground mb-3">Sichere Zahlung mit</p>
    <div className="flex items-center gap-3 flex-wrap">
      {[
        { src: '/icons/paypal.svg', alt: 'PayPal', w: 64, h: 20 },
        { src: '/icons/klarna.svg', alt: 'Klarna', w: 56, h: 20 },
        { src: '/icons/visa.svg', alt: 'Visa', w: 48, h: 16 },
        { src: '/icons/mastercard.svg', alt: 'Mastercard', w: 36, h: 22 },
      ].map((logo) => (
        <div key={logo.alt} className="bg-white rounded px-2 py-1 border border-border">
          <Image
            src={logo.src}
            alt={logo.alt}
            width={logo.w}
            height={logo.h}
            className="h-5 w-auto object-contain"
          />
        </div>
      ))}
    </div>
  </div>
</div>
```

**WICHTIG:** `sticky top-24` sorgt dafür, dass der BuyBlock auf Desktop beim Scrollen sichtbar bleibt. Der übergeordnete Container in page.tsx wird das Grid-Layout steuern — BuyBlock selbst braucht nur `sticky top-24`.
  </action>
  <verify>
    <automated>cd /Users/nicopapandrockndrock/wavyy/KofferKlar && grep -c "sticky" "app/(site)/produkt/components/BuyBlock.tsx" && grep -c "noopener" "app/(site)/produkt/components/BuyBlock.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `app/(site)/produkt/components/BuyBlock.tsx` existiert
    - Datei enthält NICHT `'use client'` (Server Component)
    - Datei enthält `sticky top-24` (PROD-09 — Desktop sticky)
    - Datei enthält `target="_blank"` und `rel="noopener noreferrer"` (PROD-08 — externer Link)
    - Datei enthält `/icons/paypal.svg`, `/icons/klarna.svg`, `/icons/visa.svg`, `/icons/mastercard.svg`
    - Datei enthält `Kostenloser Versand` (Versandinfo nach PROD-08)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>BuyBlock Server Component mit Preis, CTA-Button (externer Link), Trust-Badges, Material-Anzeige und 4 Zahlungslogos. Auf Desktop sticky.</done>
</task>

<task type="auto">
  <name>Task 2: SetOverview Server Component</name>
  <read_first>
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/lib/sanity/types.ts (SetPart interface)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/lib/sanity/image.ts (urlFor für optionale Icons)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/app/(site)/produkt/components/BuyBlock.tsx (Stil-Referenz — border, muted Tokens)
  </read_first>
  <files>
    app/(site)/produkt/components/SetOverview.tsx
  </files>
  <action>
Erstelle `app/(site)/produkt/components/SetOverview.tsx` als Server Component.

**Props-Interface:**
```typescript
import Image from 'next/image'
import { Package } from 'lucide-react'
import { urlFor } from '@/lib/sanity/image'
import type { SetPart } from '@/lib/sanity/types'

interface SetOverviewProps {
  parts: SetPart[]
}

export default function SetOverview({ parts }: SetOverviewProps)
```

**Layout:**
```tsx
<section aria-label="Set-Übersicht">
  <h2 className="font-serif text-2xl md:text-3xl font-bold text-foreground mb-8">
    Das Set im Überblick
  </h2>
  <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
    {parts.map((part, index) => (
      <div
        key={index}
        className="bg-muted rounded-xl p-4 flex flex-col items-center text-center gap-3"
      >
        {/* Icon/Bild */}
        {part.icon?.asset ? (
          <div className="relative w-16 h-16">
            <Image
              src={urlFor(part.icon).width(64).height(64).url()}
              alt={part.partName}
              fill
              className="object-contain"
              sizes="64px"
            />
          </div>
        ) : (
          <div className="w-16 h-16 flex items-center justify-center bg-border/30 rounded-lg">
            <Package className="w-8 h-8 text-muted-foreground" aria-hidden="true" />
          </div>
        )}

        {/* Name */}
        <p className="font-medium text-sm text-foreground leading-tight">{part.partName}</p>

        {/* Maße */}
        {part.dimensions && (
          <p className="text-xs text-muted-foreground">{part.dimensions}</p>
        )}
      </div>
    ))}
  </div>
</section>
```

**Fallback:** Falls `parts` leer oder undefined → nichts rendern (null).
  </action>
  <verify>
    <automated>cd /Users/nicopapandrockndrock/wavyy/KofferKlar && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `app/(site)/produkt/components/SetOverview.tsx` existiert
    - Datei enthält NICHT `'use client'`
    - Datei enthält `grid-cols-2 md:grid-cols-4` (responsives Grid)
    - Datei enthält `SetPart` Import aus `@/lib/sanity/types`
    - Datei enthält Fallback für fehlendes Icon (Package-Icon aus lucide-react)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SetOverview Server Component: Responsives 2→4-Spalten Grid der Set-Teile mit Name, Maßen und optionalem Icon/Fallback.</done>
</task>

<task type="auto">
  <name>Task 3: SizeChart Server Component</name>
  <read_first>
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/app/(site)/produkt/components/SetOverview.tsx (Stil-Konsistenz)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/.planning/REQUIREMENTS.md (PROD-04 — Größen- und Maßtabelle)
  </read_first>
  <files>
    app/(site)/produkt/components/SizeChart.tsx
  </files>
  <action>
Erstelle `app/(site)/produkt/components/SizeChart.tsx` als Server Component (KEINE Props — Maße sind hardcodiert).

**Inhalt:**
```typescript
// Keine Props — Maße ändern sich produktbedingt nicht
export default function SizeChart()
```

**Harte Maße-Daten:**
```typescript
const sizes = [
  { name: 'XL Packwürfel', dimensions: '40 × 30 × 12 cm', category: 'Würfel' },
  { name: 'L Packwürfel',  dimensions: '35 × 25 × 10 cm', category: 'Würfel' },
  { name: 'M Packwürfel',  dimensions: '30 × 20 × 8 cm',  category: 'Würfel' },
  { name: 'S Packwürfel',  dimensions: '25 × 15 × 7 cm',  category: 'Würfel' },
  { name: 'Schuhbeutel',   dimensions: '30 × 20 cm',       category: 'Flach' },
  { name: 'Wäschebeutel',  dimensions: '35 × 25 cm',       category: 'Flach' },
  { name: 'Kosmetik-Beutel', dimensions: '20 × 15 cm',     category: 'Flach' },
  { name: 'Kreditkarten-Beutel', dimensions: '12 × 9 cm',  category: 'Flach' },
]
```

**Layout:**
```tsx
<section aria-label="Maßtabelle">
  <h2 className="font-serif text-2xl md:text-3xl font-bold text-foreground mb-6">
    Maße &amp; Abmessungen
  </h2>
  {/* Horizontal scroll auf Mobile */}
  <div className="overflow-x-auto -mx-4 px-4 md:mx-0 md:px-0">
    <table className="w-full min-w-[480px] text-sm">
      <thead>
        <tr className="border-b-2 border-border">
          <th className="text-left py-3 pr-6 font-semibold text-foreground">Teil</th>
          <th className="text-left py-3 pr-6 font-semibold text-foreground">Maße (komprimiert)</th>
          <th className="text-left py-3 font-semibold text-foreground">Typ</th>
        </tr>
      </thead>
      <tbody>
        {sizes.map((size, index) => (
          <tr
            key={size.name}
            className={`border-b border-border ${index % 2 === 0 ? 'bg-background' : 'bg-muted/40'}`}
          >
            <td className="py-3 pr-6 font-medium text-foreground">{size.name}</td>
            <td className="py-3 pr-6 text-muted-foreground font-mono text-xs">{size.dimensions}</td>
            <td className="py-3 text-muted-foreground">{size.category}</td>
          </tr>
        ))}
      </tbody>
    </table>
  </div>
  <p className="mt-3 text-xs text-muted-foreground">
    * Alle Angaben im komprimierten Zustand. Entfaltet ca. 2× größer.
  </p>
</section>
```
  </action>
  <verify>
    <automated>cd /Users/nicopapandrockndrock/wavyy/KofferKlar && grep -c "<table" "app/(site)/produkt/components/SizeChart.tsx" && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `app/(site)/produkt/components/SizeChart.tsx` existiert
    - Datei enthält NICHT `'use client'`
    - Datei enthält `<table` Element (semantische Tabelle)
    - Datei enthält `overflow-x-auto` (Mobile horizontal scroll)
    - Datei enthält `40 × 30 × 12` (XL-Maße vorhanden — mind. ein Maß als Spot-Check)
    - Datei enthält `<thead>` und `<tbody>` (vollständige Tabellen-Struktur)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SizeChart Server Component: Responsive Tabelle mit 8 Packwürfel-Maßen, zebra-striping, horizontalem Scroll auf Mobile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity CMS → buyLink | buyLink-URL kommt aus Sanity — Editor-Zugang ist Basic-Auth-geschützt |
| BuyBlock → externen Shop | `noopener noreferrer` verhindert Reverse Tabnapping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Spoofing | buyLink (externer Link) | mitigate | `rel="noopener noreferrer"` verhindert Reverse Tabnapping. Link-URL kommt aus Sanity (nicht User-Input). |
| T-02-05 | Information Disclosure | Produktpreis | accept | Preis ist öffentlich — keine Schutzbedarf. Kein Auth-Gate nötig. |
</threat_model>

<verification>
Nach Task 1–3:
```bash
cd /Users/nicopapandrockndrock/wavyy/KofferKlar

# Dateien vorhanden
test -f "app/(site)/produkt/components/BuyBlock.tsx" && echo "BUYBLOCK: OK"
test -f "app/(site)/produkt/components/SetOverview.tsx" && echo "SETOVERVIEW: OK"
test -f "app/(site)/produkt/components/SizeChart.tsx" && echo "SIZECHART: OK"

# Kein 'use client' in Server Components
grep -L "'use client'" "app/(site)/produkt/components/BuyBlock.tsx" "app/(site)/produkt/components/SetOverview.tsx" "app/(site)/produkt/components/SizeChart.tsx"

# Sticky + noopener
grep "sticky" "app/(site)/produkt/components/BuyBlock.tsx"
grep "noopener" "app/(site)/produkt/components/BuyBlock.tsx"

# TypeScript + Build
npx tsc --noEmit && npm run build 2>&1 | tail -5
```
</verification>

<success_criteria>
- `BuyBlock.tsx`, `SetOverview.tsx`, `SizeChart.tsx` in `app/(site)/produkt/components/`
- Alle drei: Server Components (kein `'use client'`)
- BuyBlock: `sticky top-24`, `target="_blank" rel="noopener noreferrer"`, alle 4 Zahlungslogos
- SetOverview: responsives Grid, SetPart-Typen korrekt, Fallback-Icon
- SizeChart: semantische `<table>`, horizontal scrollbar auf Mobile, 8 Einträge
- `npm run build` exits 0
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/02-produktseite/02-02-SUMMARY.md` erstellen
</output>
