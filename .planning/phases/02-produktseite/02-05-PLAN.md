---
phase: 02-produktseite
plan: 05
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - app/(site)/produkt/page.tsx
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - PROD-03
  - PROD-04
  - PROD-05
  - PROD-06
  - PROD-07
  - PROD-08
  - PROD-09
  - PROD-10
  - PROD-11
  - PROD-12
  - DESIGN-03
  - DESIGN-05

must_haves:
  truths:
    - "app/(site)/produkt/page.tsx rendert alle 7 Komponenten: ProductGallery, BuyBlock, SetOverview, SizeChart, CompressionExplainer, VorherNachherSlider, ProductReviews, ProductFaq"
    - "Seite fetcht Product, Reviews und FaqItems parallel via Promise.all aus Sanity"
    - "JSON-LD Product-Schema ist im `<head>` vorhanden via `<script type='application/ld+json'>`"
    - "BuyBlock ist im Grid-Layout rechts neben der Galerie auf Desktop (2-Spalten-Grid), sticky"
    - "CTA-Button ist auf Mobile (375px) ohne Scrollen sichtbar — CTA kommt vor langen Content-Sektionen"
    - "metadata.title und metadata.description werden aus Sanity-Produktdaten generiert"
    - "`npm run build` exits 0 — alle Seiten kompilieren"
  artifacts:
    - path: "app/(site)/produkt/page.tsx"
      provides: "Vollständige Produktseite mit allen Komponenten, JSON-LD, Metadata"
      contains: "application/ld+json"
  key_links:
    - from: "app/(site)/produkt/page.tsx"
      to: "app/(site)/produkt/components/ProductGallery.tsx"
      via: "named import"
      pattern: "ProductGallery"
    - from: "app/(site)/produkt/page.tsx"
      to: "lib/sanity/queries.ts"
      via: "getProduct, getAllReviews, getFaqItems"
      pattern: "getProduct"
---

<objective>
Die Produktseite (`app/(site)/produkt/page.tsx`) vollständig zusammenbauen:

1. Alle Komponenten aus Plans 02-01 bis 02-04 importieren und in ein sauberes Page-Layout integrieren
2. Daten parallel aus Sanity fetchen (`Promise.all`)
3. JSON-LD Product-Schema im `<head>` einbauen (PROD-12)
4. Metadata für SEO aus Sanity-Daten generieren
5. Responsives Layout: Desktop 2-Spalten (Galerie links, BuyBlock rechts sticky), Mobile 1-Spalte mit CTA-Button before fold
6. Alle Requirements PROD-01–12, DESIGN-03, DESIGN-05 erfüllt

Purpose: Den gesamten Core Value der Website erfüllen — "Ein Besucher, der die Produktseite aufruft, versteht sofort den Nutzen, vertraut dem Anbieter und kann in wenigen Klicks kaufen."

Output: Eine vollständige, deployment-bereite Produktseite.
</objective>

<execution_context>
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.planning/ROADMAP.md
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.planning/REQUIREMENTS.md
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.planning/phases/01-foundation-cms/01-CONTEXT.md

<interfaces>
<!-- Verfügbare Queries aus lib/sanity/queries.ts -->
```typescript
import { getProduct, getAllReviews, getFaqItems } from '@/lib/sanity/queries'
// getProduct() → Product | null
// getAllReviews() → Review[]
// getFaqItems() → FaqItem[]
// Alle mit next: { revalidate: 3600 } (ISR, stündlich)
```

<!-- Seitenstruktur — Mobile-First (DESIGN-05: CTA above fold auf 375px) -->
// Mobile: Galerie → CTA-Block (kompakt, kein sticky) → Rest der Inhalte
// Desktop: 2-Spalten-Grid: [Galerie, 60%] [BuyBlock, 40%, sticky]
// Desktop-Sticky: BuyBlock hat `sticky top-24` intern — Page braucht `items-start` Grid

<!-- JSON-LD Product Schema (PROD-12) -->
// Minimal-Schema:
// {
//   "@context": "https://schema.org",
//   "@type": "Product",
//   "name": product.name,
//   "description": product.shortDescription,
//   "offers": {
//     "@type": "Offer",
//     "price": product.price,
//     "priceCurrency": "EUR",
//     "availability": "https://schema.org/InStock"
//   },
//   "aggregateRating": {  // nur wenn reviews.length > 0
//     "@type": "AggregateRating",
//     "ratingValue": avgRating,
//     "reviewCount": reviews.length,
//     "bestRating": 5,
//     "worstRating": 1
//   }
// }

<!-- Page-Level Caching (CLAUDE.md) -->
// export const revalidate = 3600  // ISR, stündlich
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produktseite assemblieren + JSON-LD + Metadata</name>
  <read_first>
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/app/(site)/produkt/page.tsx (aktueller Skeleton — wird vollständig ersetzt)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/lib/sanity/queries.ts (getProduct, getAllReviews, getFaqItems — Signaturen prüfen)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/lib/sanity/types.ts (Product, Review, FaqItem — alle Felder)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/app/(site)/produkt/components/ProductGallery.tsx (Props-Interface)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/app/(site)/produkt/components/BuyBlock.tsx (Props-Interface)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/app/(site)/produkt/components/SetOverview.tsx (Props-Interface)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/app/(site)/produkt/components/SizeChart.tsx (keine Props)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/app/(site)/produkt/components/CompressionExplainer.tsx (Props-Interface)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/app/(site)/produkt/components/VorherNachherSlider.tsx (Props-Interface)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/app/(site)/produkt/components/ProductReviews.tsx (Props-Interface)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/app/(site)/produkt/components/ProductFaq.tsx (Props-Interface)
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/.agents/skills/high-end-visual-design/SKILL.md
    - /Users/nicopapandrockndrock/wavyy/KofferKlar/.agents/skills/design-taste-frontend/SKILL.md
  </read_first>
  <files>
    app/(site)/produkt/page.tsx
  </files>
  <action>
Ersetze den Skeleton in `app/(site)/produkt/page.tsx` mit der vollständigen Implementierung.

**ISR-Konfiguration:**
```typescript
export const revalidate = 3600
```

**Imports:**
```typescript
import type { Metadata } from 'next'
import { getProduct, getAllReviews, getFaqItems } from '@/lib/sanity/queries'
import ProductGallery from './components/ProductGallery'
import ProductVideo from './components/ProductVideo'
import BuyBlock from './components/BuyBlock'
import SetOverview from './components/SetOverview'
import SizeChart from './components/SizeChart'
import CompressionExplainer from './components/CompressionExplainer'
import VorherNachherSlider from './components/VorherNachherSlider'
import ProductReviews from './components/ProductReviews'
import ProductFaq from './components/ProductFaq'
```

**Metadata-Generierung:**
```typescript
export async function generateMetadata(): Promise<Metadata> {
  const product = await getProduct()
  return {
    title: product?.seo?.title ?? product?.name ?? 'KofferKlar Packwürfel-Set',
    description:
      product?.seo?.description ??
      product?.shortDescription ??
      'Das 8-teilige Kompressions-Packwürfel-Set für stressfreies Reisen.',
  }
}
```

**Durchschnitt-Berechnung (für JSON-LD):**
```typescript
function calcAvgRating(reviews: { rating: number }[]): number {
  if (!reviews.length) return 0
  return Math.round((reviews.reduce((s, r) => s + r.rating, 0) / reviews.length) * 10) / 10
}
```

**JSON-LD Helper:**
```typescript
function buildProductJsonLd(product: NonNullable<Awaited<ReturnType<typeof getProduct>>>, reviews: Awaited<ReturnType<typeof getAllReviews>>) {
  const avg = calcAvgRating(reviews)
  const schema: Record<string, unknown> = {
    '@context': 'https://schema.org',
    '@type': 'Product',
    name: product.name,
    description: product.shortDescription ?? '',
    offers: {
      '@type': 'Offer',
      price: product.price ?? 0,
      priceCurrency: 'EUR',
      availability: 'https://schema.org/InStock',
      url: 'https://kofferklar.de/produkt',
    },
  }
  if (reviews.length > 0) {
    schema.aggregateRating = {
      '@type': 'AggregateRating',
      ratingValue: avg,
      reviewCount: reviews.length,
      bestRating: 5,
      worstRating: 1,
    }
  }
  return JSON.stringify(schema)
}
```

**Page-Komponente:**
```typescript
export default async function ProduktPage() {
  // Paralleles Fetching
  const [product, reviews, faqItems] = await Promise.all([
    getProduct(),
    getAllReviews(),
    getFaqItems(),
  ])

  return (
    <>
      {/* JSON-LD */}
      {product && (
        <script
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: buildProductJsonLd(product, reviews) }}
        />
      )}

      <main className="min-h-screen pt-[72px]">

        {/* ─── Hero-Sektion: Galerie + BuyBlock ─── */}
        <section className="max-w-[1400px] mx-auto px-4 md:px-8 py-8 md:py-16">
          {/* Mobile: CTA zuerst (above fold) */}
          <div className="md:hidden mb-6">
            <h1 className="font-serif text-2xl font-bold text-foreground mb-2">
              {product?.name ?? 'KofferKlar Packwürfel-Set'}
            </h1>
            {product?.shortDescription && (
              <p className="text-muted-foreground text-sm mb-4">{product.shortDescription}</p>
            )}
            <BuyBlock
              price={product?.price}
              buyLink={product?.buyLink}
              material={product?.material}
            />
          </div>

          {/* Desktop: 2-Spalten-Grid */}
          <div className="hidden md:grid md:grid-cols-[1fr_380px] gap-12 items-start">
            {/* Links: Galerie + Video */}
            <div className="space-y-6">
              <h1 className="font-serif text-3xl md:text-4xl font-bold text-foreground">
                {product?.name ?? 'KofferKlar Packwürfel-Set'}
              </h1>
              {product?.shortDescription && (
                <p className="text-muted-foreground text-base leading-relaxed">
                  {product.shortDescription}
                </p>
              )}
              <ProductGallery
                images={product?.images ?? []}
                productName={product?.name ?? 'Produkt'}
              />
              {product?.videoUrl && (
                <ProductVideo videoUrl={product.videoUrl} title={product.name} />
              )}
            </div>

            {/* Rechts: BuyBlock (sticky) */}
            <div>
              <BuyBlock
                price={product?.price}
                buyLink={product?.buyLink}
                material={product?.material}
              />
            </div>
          </div>

          {/* Mobile: Galerie nach CTA */}
          <div className="md:hidden">
            <ProductGallery
              images={product?.images ?? []}
              productName={product?.name ?? 'Produkt'}
            />
            {product?.videoUrl && (
              <div className="mt-4">
                <ProductVideo videoUrl={product.videoUrl} title={product.name} />
              </div>
            )}
          </div>
        </section>

        {/* ─── Set-Übersicht ─── */}
        {product?.setParts && product.setParts.length > 0 && (
          <section className="bg-muted/50 py-16">
            <div className="max-w-[1400px] mx-auto px-4 md:px-8">
              <SetOverview parts={product.setParts} />
            </div>
          </section>
        )}

        {/* ─── Maßtabelle ─── */}
        <section className="max-w-[1400px] mx-auto px-4 md:px-8 py-16">
          <SizeChart />
        </section>

        {/* ─── Kompressions-Erklärung ─── */}
        <section className="bg-muted/30 py-16">
          <div className="max-w-[1400px] mx-auto px-4 md:px-8">
            <CompressionExplainer />
          </div>
        </section>

        {/* ─── Vorher/Nachher-Slider ─── */}
        <section className="max-w-[1400px] mx-auto px-4 md:px-8 py-16">
          <VorherNachherSlider />
        </section>

        {/* ─── Kundenbewertungen ─── */}
        {reviews.length > 0 && (
          <section className="bg-muted/30 py-16">
            <div className="max-w-[1400px] mx-auto px-4 md:px-8">
              <ProductReviews reviews={reviews} />
            </div>
          </section>
        )}

        {/* ─── FAQ ─── */}
        {faqItems.length > 0 && (
          <section className="max-w-[1400px] mx-auto px-4 md:px-8 py-16">
            <ProductFaq items={faqItems} />
          </section>
        )}

        {/* ─── Bottom CTA ─── */}
        <section className="bg-primary py-16">
          <div className="max-w-[1400px] mx-auto px-4 md:px-8 text-center">
            <h2 className="font-serif text-3xl md:text-4xl font-bold text-white mb-4">
              Bereit für stressfreies Reisen?
            </h2>
            <p className="text-white/80 mb-8 max-w-md mx-auto">
              8-teiliges Kompressions-Packwürfel-Set — jetzt versandkostenfrei bestellen.
            </p>
            <a
              href={product?.buyLink ?? '#'}
              target="_blank"
              rel="noopener noreferrer"
              className="inline-block bg-accent text-accent-foreground px-10 py-4 rounded-full text-base font-semibold hover:bg-accent-400 active:scale-[0.98] transition-all duration-300 focus-visible:ring-2 focus-visible:ring-white focus-visible:ring-offset-2 focus-visible:ring-offset-primary"
              style={{ transitionTimingFunction: 'cubic-bezier(0.16, 1, 0.3, 1)' }}
            >
              Jetzt kaufen
            </a>
          </div>
        </section>

      </main>
    </>
  )
}
```

**WICHTIGE HINWEISE:**
1. `dangerouslySetInnerHTML` für JSON-LD ist sicher — der Content wird von `buildProductJsonLd` kontrolliert (kein User-Input)
2. `Promise.all` fetcht alle drei Datenquellen parallel → keine sequenzielle Waterfall-Fetching
3. Mobile: BuyBlock erscheint ZUERST (vor Galerie) damit CTA above fold auf 375px (DESIGN-05)
4. Desktop: `grid-cols-[1fr_380px]` — Galerie breit, BuyBlock schmal und sticky
5. Alle Sections mit `null`-Guards — leere States crashen die Seite nicht
6. Wenn TypeScript-Fehler bei den PLAN.md-Props auftreten: Props-Typen aus den jeweiligen Komponenten lesen und anpassen
  </action>
  <verify>
    <automated>
cd /Users/nicopapandrockndrock/wavyy/KofferKlar
# JSON-LD vorhanden
grep -c "application/ld+json" "app/(site)/produkt/page.tsx"
# Promise.all vorhanden
grep -c "Promise.all" "app/(site)/produkt/page.tsx"
# TypeScript
npx tsc --noEmit 2>&1 | head -20
# Build
npm run build 2>&1 | tail -15
    </automated>
  </verify>
  <acceptance_criteria>
    - `app/(site)/produkt/page.tsx` enthält `application/ld+json` (PROD-12)
    - Datei enthält `@type.*Product` im JSON-LD-String (valides Product-Schema)
    - Datei enthält `aggregateRating` (Rating in JSON-LD)
    - Datei enthält `Promise.all` (paralleles Fetching)
    - Datei enthält `export const revalidate = 3600` (ISR)
    - Datei enthält `generateMetadata` (SEO-Metadata)
    - Datei enthält alle 8 Komponenten-Imports: ProductGallery, ProductVideo, BuyBlock, SetOverview, SizeChart, CompressionExplainer, VorherNachherSlider, ProductReviews, ProductFaq
    - Datei enthält `noopener noreferrer` am Bottom-CTA
    - Datei enthält `md:grid-cols-` (responsives Grid Desktop/Mobile)
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Vollständige Produktseite: Alle 8 Komponenten integriert, paralleles Sanity-Fetching, JSON-LD Product Schema, SEO-Metadata, responsives 2-Spalten-Desktop-Layout mit sticky BuyBlock, Mobile CTA above fold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sanity CMS → JSON-LD | buildProductJsonLd() serialisiert Sanity-Daten — keine User-Input-Daten im Schema |
| JSON-LD → dangerouslySetInnerHTML | Kontrollierte Daten, kein XSS-Risiko |
| buyLink → externen Shop | noopener noreferrer verhindert Reverse Tabnapping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | XSS | JSON-LD via dangerouslySetInnerHTML | accept | Daten kommen ausschließlich aus Sanity (server-seitig, kein User-Input). JSON.stringify() escaped Sonderzeichen. |
| T-02-11 | Spoofing | buyLink externer Redirect | accept | noopener noreferrer gesetzt. URL-Validierung erfolgt durch Sanity URL-Field-Type. |
| T-02-12 | Information Disclosure | Preis im JSON-LD | accept | Preis ist öffentlich auf der Seite sichtbar — keine zusätzliche Disclosure durch JSON-LD. |
</threat_model>

<verification>
Nach Task 1:
```bash
cd /Users/nicopapandrockndrock/wavyy/KofferKlar

# JSON-LD
grep "application/ld+json" "app/(site)/produkt/page.tsx" && echo "JSON-LD: OK"
grep "aggregateRating" "app/(site)/produkt/page.tsx" && echo "RATING: OK"

# Promise.all
grep "Promise.all" "app/(site)/produkt/page.tsx" && echo "PARALLEL-FETCH: OK"

# ISR
grep "revalidate = 3600" "app/(site)/produkt/page.tsx" && echo "ISR: OK"

# Alle Komponenten-Imports
grep -c "from './components/" "app/(site)/produkt/page.tsx"  # Muss ≥ 8 sein

# TypeScript
npx tsc --noEmit 2>&1 | grep -c "error" || true  # Muss 0 sein

# Build
npm run build 2>&1 | tail -10
```
</verification>

<success_criteria>
- `app/(site)/produkt/page.tsx` vollständig implementiert
- JSON-LD Product-Schema im `<head>` mit name, price, currency, aggregateRating
- Paralleles Fetching via `Promise.all([getProduct(), getAllReviews(), getFaqItems()])`
- ISR: `export const revalidate = 3600`
- generateMetadata aus Sanity-Produktdaten
- Alle 8 Komponenten importiert und gerendert
- Desktop: 2-Spalten-Grid, BuyBlock rechts sticky
- Mobile: BuyBlock (CTA) vor Galerie = above fold auf 375px
- `npm run build` exits 0 mit allen Seiten kompiliert
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/02-produktseite/02-05-SUMMARY.md` erstellen
</output>
