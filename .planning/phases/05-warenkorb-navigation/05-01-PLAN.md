---
phase: 05-warenkorb-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(site)/components/CartProvider.tsx
autonomous: true
requirements:
  - CART-01
  - CART-02
  - CART-05

must_haves:
  truths:
    - "CartProvider wraps the site layout; useCart() returns items, addToCart, updateQty, removeItem, clearCart, totalItems, totalPrice, mounted"
    - "Adding the same color twice increases qty, not duplicate rows"
    - "Cart state survives a page reload via localStorage key kofferklar-cart"
    - "useCart() throws a readable error when called outside CartProvider"
    - "Badge consumers can suppress SSR flicker using the mounted boolean"
  artifacts:
    - path: "app/(site)/components/CartProvider.tsx"
      provides: "CartContext, CartProvider component, useCart hook, CartItem type"
      exports: ["CartItem", "CartProvider", "useCart"]
      contains: "kofferklar-cart"
  key_links:
    - from: "app/(site)/layout.tsx"
      to: "app/(site)/components/CartProvider.tsx"
      via: "named import { CartProvider }"
      pattern: "CartProvider"
    - from: "app/(site)/components/CartProvider.tsx"
      to: "localStorage"
      via: "useEffect after mount"
      pattern: "localStorage\\.setItem.*kofferklar-cart"
---

<objective>
Create the CartProvider — the global cart state foundation that all other plans in this phase depend on.

Purpose: Centralise cart state in React Context with localStorage persistence. Every component that needs to add items, read the badge count, or display the cart reads from this one file. No component should manage its own cart state.

Output: `app/(site)/components/CartProvider.tsx` — fully working, hydration-safe, typed.
</objective>

<execution_context>
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nicopapandrockndrock/wavyy/KofferKlar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-warenkorb-navigation/05-RESEARCH.md
@.planning/phases/05-warenkorb-navigation/05-PATTERNS.md

<interfaces>
<!-- CartItem type and CartContextValue interface the executor creates — must match exactly. -->
<!-- All downstream plans (02, 03, 04) import from this file. -->

CartItem (to export):
```typescript
export interface CartItem {
  color: string        // e.g. "Aqua Blau" — used as the unique key
  colorLabel: string   // display label; same value as color in v1
  qty: number          // always >= 1; never undefined
  price: number        // EUR, snapshotted at add time
}
```

CartContextValue (internal interface):
```typescript
interface CartContextValue {
  items: CartItem[]
  addToCart: (item: Omit<CartItem, 'qty'> & { qty?: number }) => void
  updateQty: (color: string, qty: number) => void
  removeItem: (color: string) => void
  clearCart: () => void
  totalItems: number   // sum of all item.qty
  totalPrice: number   // sum of all item.price * item.qty
  mounted: boolean     // false during SSR; true after useEffect runs — badge consumers MUST gate on this
}
```

Exports from CartProvider.tsx:
- `export interface CartItem` — type used by all consumers
- `export function CartProvider` — wraps (site) layout
- `export function useCart(): CartContextValue` — throws if used outside provider
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CartProvider.tsx — hydration-safe cart context with localStorage persistence</name>
  <files>app/(site)/components/CartProvider.tsx</files>
  <read_first>
    - app/(site)/components/MobileDrawer.tsx — useEffect + cleanup-return shape to mirror
    - app/(site)/components/Header.tsx — confirms 'use client' placement pattern (line 1)
    - .planning/phases/05-warenkorb-navigation/05-RESEARCH.md — full CartProvider code in §Code Examples
    - .planning/phases/05-warenkorb-navigation/05-PATTERNS.md — Pattern assignment for CartProvider.tsx
  </read_first>
  <action>
Create `app/(site)/components/CartProvider.tsx` as a new file. Use the exact implementation from RESEARCH.md §Code Examples "Full CartProvider (production-ready)".

Critical invariants — do NOT deviate:

1. First line MUST be `'use client'` (before all imports) — matches MobileDrawer.tsx L1 pattern.

2. `useState<CartItem[]>([])` — always initialise with empty array. NEVER read localStorage in the state initialiser. Reason: localStorage is undefined on the Node.js server; any read in useState() causes a hydration mismatch error (RESEARCH.md Pitfall 1).

3. Two separate useEffects:
   - First: reads localStorage ONCE on mount, then calls `setMounted(true)`. No deps except `[]`.
   - Second: writes `JSON.stringify(items)` to `localStorage.setItem('kofferklar-cart', ...)` on every items change. Gate with `if (!mounted) return` so it does NOT write before the first read completes.

4. `addToCart` merge logic (CART-05): before appending a new item, check `prev.findIndex(i => i.color === incoming.color)`. If found, increment qty. If not found, append. Use `incoming.qty ?? 1` for the amount.

5. `updateQty` guard: `if (qty < 1) return` before updating state — prevents negative qty.

6. `useCart()` must throw: `if (!ctx) throw new Error('useCart must be used inside CartProvider')`.

7. Export `CartItem` as a named interface export — downstream plans import the type from this file.

8. Export `CartProvider` and `useCart` as named function exports (not default).

STORAGE_KEY constant: `const STORAGE_KEY = 'kofferklar-cart'`

The `mounted: boolean` field MUST be in both the `CartContextValue` interface and in the Provider value object. Badge consumers in Header.tsx use this to suppress the badge count during SSR.
  </action>
  <verify>
    <automated>
      cd /Users/nicopapandrockndrock/wavyy/KofferKlar && npx tsc --noEmit 2>&1 | head -30
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -n "export interface CartItem" app/(site)/components/CartProvider.tsx — must find exactly 1 match
    - grep -n "export function CartProvider" app/(site)/components/CartProvider.tsx — must find exactly 1 match
    - grep -n "export function useCart" app/(site)/components/CartProvider.tsx — must find exactly 1 match
    - grep -n "kofferklar-cart" app/(site)/components/CartProvider.tsx — must find STORAGE_KEY declaration
    - grep -n "mounted" app/(site)/components/CartProvider.tsx — must find at least 3 matches (interface, useState, Provider value)
    - grep -n "useState<CartItem\[\]>(\[\])" app/(site)/components/CartProvider.tsx — must find exactly 1 match (no localStorage in initialiser)
    - grep -n "useCart must be used inside CartProvider" app/(site)/components/CartProvider.tsx — must find exactly 1 match
    - grep -n "findIndex" app/(site)/components/CartProvider.tsx — must find merge logic in addToCart
    - TypeScript: npx tsc --noEmit exits with 0 (no type errors in CartProvider.tsx)
  </acceptance_criteria>
  <done>CartProvider.tsx exists, TypeScript compiles clean, all exports present, hydration-safe pattern implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Wire CartProvider into app/(site)/layout.tsx</name>
  <files>app/(site)/layout.tsx</files>
  <read_first>
    - app/(site)/layout.tsx — current 18-line file; must remain a Server Component (no 'use client')
    - app/(site)/components/CartProvider.tsx — the file just created in Task 1
  </read_first>
  <action>
Modify `app/(site)/layout.tsx` to import and use `CartProvider`.

Changes:
1. Add import: `import { CartProvider } from './components/CartProvider'`
2. Wrap the existing content — `Header`, `{children}`, and `Footer` — inside `<CartProvider>...</CartProvider>`.

The accessibility skip-link (`<a href="#main-content" ...>`) stays outside CartProvider — it belongs at the very top of the layout fragment, unchanged.

CRITICAL: Do NOT add `'use client'` to layout.tsx. The layout stays a Server Component. CartProvider is a Client Component imported into it — this is the standard Next.js 15 App Router pattern (RESEARCH.md Pattern 2, verified against nextjs.org).

Result structure:
```
<>
  <a href="#main-content" ...>Zum Hauptinhalt springen</a>
  <CartProvider>
    <Header />
    {children}
    <Footer />
  </CartProvider>
</>
```
  </action>
  <verify>
    <automated>
      cd /Users/nicopapandrockndrock/wavyy/KofferKlar && npx tsc --noEmit 2>&1 | head -30
    </automated>
  </verify>
  <acceptance_criteria>
    - grep -n "CartProvider" app/(site)/layout.tsx — must find import line AND wrapping JSX (at least 2 matches)
    - grep -n "'use client'" app/(site)/layout.tsx — must find ZERO matches (layout stays Server Component)
    - grep -n "from './components/CartProvider'" app/(site)/layout.tsx — must find exactly 1 match
    - grep -n "<Header />" app/(site)/layout.tsx — must still be present (not removed)
    - grep -n "<Footer" app/(site)/layout.tsx — must still be present
    - TypeScript: npx tsc --noEmit exits with 0
  </acceptance_criteria>
  <done>layout.tsx imports CartProvider, wraps Header+children+Footer in it, no 'use client' directive on layout, TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage → CartContext | User-controlled storage; data is parsed and loaded into state on mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | CartProvider localStorage read | mitigate | JSON.parse wrapped in try/catch; corrupt data silently ignored (empty array fallback) |
| T-05-02 | Tampering | CartItem.price from localStorage | accept | Price snapshotted at add time; no real payment backend in v1 — acceptable until payment backend added |
| T-05-03 | Tampering | CartItem.qty via localStorage | mitigate | updateQty guards qty < 1; addToCart uses qty ?? 1; qty is never NaN or negative in valid state |
| T-05-04 | Information Disclosure | localStorage key kofferklar-cart | accept | Cart data contains color names and prices only — no PII, no payment data, low-value target |
</threat_model>

<verification>
After both tasks:
1. `npx tsc --noEmit` — exits 0, no errors in CartProvider.tsx or layout.tsx
2. `npm run dev` starts without errors
3. `grep -n "CartProvider" app/(site)/layout.tsx` — shows import and JSX wrapping
4. `grep -n "'use client'" app/(site)/layout.tsx` — zero matches
5. `grep -n "mounted" app/(site)/components/CartProvider.tsx` — at least 3 matches
</verification>

<success_criteria>
- CartProvider.tsx created with all exports: CartItem, CartProvider, useCart
- layout.tsx wraps Header/children/Footer in CartProvider
- TypeScript compiles with zero errors
- mounted boolean exposed in context value
- localStorage only accessed in useEffect (never in useState initializer)
</success_criteria>

<output>
After completion, create `.planning/phases/05-warenkorb-navigation/05-01-SUMMARY.md`
</output>
